import React, { FC } from 'react';
import { Container } from '../../shared/styled/containers/Container';

const Profile: FC = () => {
  return <Container>This is profile page</Container>;
};

export default Profile;
