import React, { FC } from 'react';
import { Container } from '../../shared/styled/containers/Container';

const Cart: FC = () => {
  return <Container>This is cart page</Container>;
};

export default Cart;
