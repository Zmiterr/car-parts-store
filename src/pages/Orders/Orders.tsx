import React, { FC } from 'react';
import { Container } from '../../shared/styled/containers/Container';

const Orders: FC = () => {
  return <Container>This is orders page</Container>;
};

export default Orders;
