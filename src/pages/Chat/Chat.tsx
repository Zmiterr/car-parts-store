import React, { FC } from 'react';
import { Container } from '../../shared/styled/containers/Container';

const Chat: FC = () => {
  return <Container>This is chat page</Container>;
};

export default Chat;
