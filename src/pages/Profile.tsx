import React, { FC } from 'react';
import { StyledContainer } from '../theme/container';

const Profile: FC = () => {
  return <StyledContainer>This is profile page</StyledContainer>;
};

export default Profile;
